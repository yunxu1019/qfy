function main() {
    var page = div();
    page.innerHTML = welist;
    render(page, {
        btn: button,
        edit(data = {}) {
            cast(page, data);
        },
        padding,
    });
    return page;
}